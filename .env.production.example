# Production: leave empty when frontend and API are on the same domain (recommended).
# The app will call /api/... on the same host.
REACT_APP_API_URL=

# Only set this if the API is on a different domain, e.g.:
# REACT_APP_API_URL=https://api.your-domain.com

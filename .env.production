# tapreward.jitihada.co.tz — use same-origin /api/* (nginx must proxy /api to PHP).
# See deploy/nginx.tapreward-subdomain.conf.example
REACT_APP_USE_SAME_ORIGIN=true

# Only use a separate API URL if frontend cannot proxy /api (requires backend CORS):
# REACT_APP_API_URL=http://tap.jitihada.co.tz

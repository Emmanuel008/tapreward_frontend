# Leave empty in development — requests use /api/* via proxy (package.json → live server).
# For production on the same domain, leave empty too.
REACT_APP_API_URL=

# For production on a different domain:
# REACT_APP_API_URL=http://tap.jitihada.co.tz
